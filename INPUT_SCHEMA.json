{
  "title": "PDF Signer Input",
  "description": "Input configuration for the PDF Signer actor, which converts an XLSX file to PDF and adds a signature and date.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "xlsx_key": {
      "title": "XLSX File Key",
      "type": "string",
      "description": "The key for the XLSX file stored in the Apify key-value store.",
      "editor": "textfield"
    },
    "signature_key": {
      "title": "Signature Image Key",
      "type": "string",
      "description": "The key for the signature image stored in the Apify key-value store.",
      "default": "signature.png",
      "editor": "textfield"
    },
    "blank_image_key": {
      "title": "Blank Image Key",
      "type": "string",
      "description": "The key for the blank image used to cover existing dates in the PDF, stored in the Apify key-value store.",
      "default": "line.png",
      "editor": "textfield"
    }
  },
  "required": ["xlsx_key"]
}